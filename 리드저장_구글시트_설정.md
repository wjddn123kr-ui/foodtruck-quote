# 📋 리드 자동저장 — 구글시트 연결 (사장님용)

챗봇/견적폼에서 들어온 상담(연락처·메뉴·견적)을 **구글시트에 자동으로 한 줄씩 저장**하는 설정입니다.
코드는 이미 배포됐고, 아래 **딱 한 번** 세팅만 하면 됩니다. (약 5분)

---

## 1단계 — 구글시트 만들기
1. https://sheets.google.com 접속 → **빈 스프레드시트** 새로 만들기.
2. 시트 이름을 알아보기 쉽게 (예: `푸드트럭 상담 리드`).

## 2단계 — Apps Script 붙여넣기
1. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭.
2. 열린 편집기의 코드를 **전부 지우고**, 아래 코드를 **그대로 붙여넣기**:

```javascript
function doPost(e) {
  try {
    var ss = SpreadsheetApp.getActiveSpreadsheet();
    var sheet = ss.getSheetByName('leads') || ss.insertSheet('leads');
    if (sheet.getLastRow() === 0) {
      sheet.appendRow(['접수시각','경로','담당자/회사','연락처','지역','메뉴/인분','견적금액','일정','문의/특이사항']);
    }
    var d = JSON.parse(e.postData.contents);
    sheet.appendRow([
      new Date(),
      d.source || '',
      d.contactName || '',
      d.phone || '',
      d.region || '',
      d.menu || '',
      d.quote || '',
      d.datetime || '',
      d.note || ''
    ]);
    return ContentService
      .createTextOutput(JSON.stringify({ ok: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (err) {
    return ContentService
      .createTextOutput(JSON.stringify({ ok: false, error: String(err) }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}
```

3. 💾 저장(디스크 아이콘).

## 3단계 — 웹앱으로 배포
1. 우측 상단 **배포 → 새 배포**.
2. 톱니바퀴(유형 선택) → **웹 앱** 선택.
3. 설정:
   - **실행 계정(다음 사용자로 실행):** 나
   - **액세스 권한(액세스 권한이 있는 사용자):** **모든 사용자**  ← 꼭 이걸로!
4. **배포** 클릭 → 구글 로그인/권한 승인(“고급 → 안전하지 않음(이동)” 눌러 진행).
5. 나오는 **웹 앱 URL**을 복사. (`https://script.google.com/macros/s/………/exec` 형태)

## 4단계 — Vercel 환경변수 등록
1. https://vercel.com → 프로젝트 **foodtruck-quote** → **Settings → Environment Variables**.
2. 새 변수 추가:
   - **Name:** `SHEET_WEBHOOK_URL`
   - **Value:** 3단계에서 복사한 웹 앱 URL
   - **Environment:** **Production** 체크 (Preview도 같이 체크해두면 좋음)
3. 저장 → **Deployments** 탭에서 최신 배포를 **Redeploy**(재배포). (환경변수는 재배포해야 반영됨)

## 5단계 — 저에게 알려주기
“시트 연결 끝” 하고 알려주시면, 제가 **테스트 상담 1건**을 실제로 넣어서
- 시트에 줄이 잘 쌓이는지
- 담당자 문자도 잘 오는지
둘 다 한 번에 확인해 드립니다.

---

### 참고
- 시트 시간대가 이상하면: Apps Script 편집기 **프로젝트 설정(톱니) → 시간대 → (GMT+09:00) 서울** 로 맞추세요.
- 저장은 **선택형 견적폼**과 **대화형 챗봇** 양쪽 모두에서 자동으로 쌓입니다.
- 이 저장 기능은 문자 알림과 **독립적**입니다. 문자가 실패해도 시트 기록은 남고, 시트가 실패해도 문자는 나갑니다. (리드를 이중으로 안 잃게)
