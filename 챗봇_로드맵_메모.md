# 🤖 챗봇 로드맵 · 다음 단계 메모

> 대화형 챗봇을 "안내용"에서 "영업 도구"로 키우는 로드맵. (2026-07-03 기준)

---

## ✅ A. 연락처 수집 + 직원 알림 — **완료(구현됨)**
- 챗봇이 상담 끝에 **성함(회사명) + 연락처**를 받으면, 담당자에게 **문자(LMS)로 자동 전달**.
- 구조: 챗봇이 `submit_lead` 도구 호출 → [lib/lead-notify.js](lib/lead-notify.js)가 솔라피(SOLAPI)로 문자 발송.
- ⚠️ **작동 조건**: Vercel에 아래 환경변수가 있어야 문자가 실제로 나감. (없으면 대화상 접수는 되지만 문자 미발송)
  - `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, `SOLAPI_SENDER`(발신번호), `ALERT_TO`(직원 수신번호)
  - 이미 기존 견적폼(api/notify.js)에서 쓰던 값 → **Preview 환경에도 적용돼 있는지 확인 필요**.

---

## 🟡 B. 리드 자동저장 (구글시트) — **✅ 코드 구현·배포 완료 / 시트 세팅만 남음 (2026-08-11)**
- 구현: `lib/sheet-log.js`(SHEET_WEBHOOK_URL로 append) + `api/notify.js`(선택형) + `api/chat.js`(대화형)에서 저장.
- 남은 것: 사장님이 구글시트+Apps Script 웹앱 만들고 `SHEET_WEBHOOK_URL` 환경변수 등록 → 재배포. **가이드: [리드저장_구글시트_설정.md](리드저장_구글시트_설정.md)**
- env 없으면 저장은 건너뛰고 문자 알림은 그대로(안전). 세팅 후 E2E 테스트 예정.
- (아래는 원래 Supabase 계획 — 규모 커지면 이관 고려)

## 🟡 B(원안). 대화 기록 자동 백업 (Supabase) — 참고용

### 개념
- **Supabase = 무료로 시작 가능한 온라인 데이터베이스(DB)**. 여기에 상담 내용을 저장하면, 사이트가 꺼져 있어도 **자동으로 영구 백업**되고 나중에 조회 가능.
- "맞다, Supabase로 연동하면 자동 백업된다" → 정확합니다. 단 **아직 그 단계 세팅을 안 해서** 지금은 저장 안 되고 있음(대화가 증발). A(문자 알림)는 실시간 전달, B(DB 저장)는 영구 기록 — 둘은 상호보완.

### 무엇을 저장할까 (테이블 컬럼 예시: `leads`)
| 컬럼 | 내용 |
|------|------|
| id | 자동 번호 |
| created_at | 접수 시각 |
| contact_name | 담당자/회사명 |
| phone | 연락처 |
| region | 지역 |
| menu | 메뉴·인분 |
| quote | 견적 금액 |
| datetime | 행사 일정 |
| note | 특이사항 |
| transcript | 전체 대화 원문(선택) |

### 세팅 순서 (나중에 진행)
1. [supabase.com](https://supabase.com) 가입 → 새 프로젝트 생성(무료).
2. `leads` 테이블 생성(위 컬럼).
3. 프로젝트의 **URL + service_role 키**를 Vercel 환경변수로 등록
   (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`).
4. [api/chat.js](api/chat.js)의 `submit_lead` 처리부에서 문자 발송과 **동시에** Supabase에 insert.
   → 즉 A와 같은 지점에서 "문자 보내기 + DB 저장"을 함께 실행하면 됨.
5. (선택) Supabase 대시보드에서 바로 리드 목록 조회, 또는 관리자 페이지 별도 제작.

### 대안(더 쉬움)
- **구글 시트**에 저장(Apps Script 웹앱 or 연동). 직원들이 엑셀처럼 바로 봄. DB보다 간단하지만 검색·확장성은 약함.
- 추천: **처음엔 구글 시트**로 가볍게 → 규모 커지면 Supabase로.

---

## 🟢 C. 챗봇 UX 개선 — **천천히 / 사장님이 줄 자료**
- **메뉴판 보여주기** ("메뉴판 보여줘" → 이미지/링크)
  - 필요 자료: 메뉴판 이미지(이미 바탕화면 `메뉴판_섹션별_사진` 있음) **또는** 홈페이지 메뉴 페이지 URL
- **견적 결과를 예쁜 카드**로 (자료 불필요, 디자인만)
- **전화/카톡 상담 버튼** (바로 연결 CTA)
  - 필요 자료: **카카오톡 채널 주소**(있으면), 전화번호(1877-6901 확보됨)
- **"새 상담 시작" 버튼** (자료 불필요)

👉 C에서 사장님이 주면 좋은 것: ① 카카오톡 채널 URL, ② 메뉴판 이미지 or 메뉴 페이지 주소.

---

## 🔵 D. 홈페이지 연동 — **방향 확정**
- 사장님 제안: **홈페이지 "3초 견적내기"(선택형 챗봇)를 기본으로 두고, "대화형으로 상담하기" 버튼을 눌러 대화형 챗봇으로 전환.**
- ✅ 좋은 방향. 선택형(빠름) + 대화형(자유질문) 둘 다 살림.
- 진행 방법(나중):
  1. 홈페이지(foodtruck-home)의 3초 견적 영역에 **"대화로 상담하기" 버튼** 추가.
  2. 버튼 클릭 → 대화형 챗봇(chatbot.html) 열기(새 창 or 팝업/위젯).
  3. 반대로 챗봇 안에도 "빠른 선택형 견적" 링크를 둘 수 있음.
- ⚠️ 이건 **다른 저장소(foodtruck-home)** 작업이라, 진행 시 그쪽 파일을 다뤄야 함.

---

## 진행 우선순위(제안)
1. **A 테스트** (환경변수 확인 → 실제 문자 오는지)
2. **B** 구글시트 or Supabase (A와 같은 지점에 저장 추가)
3. **D** 홈페이지 버튼 연동
4. **C** UX 하나씩
